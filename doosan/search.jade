doctype html 
html 
    head 
        title Main Page 
        meta(charset='utf-8')
        style
            include ../stylesheets/mainpage.css
    body.main-area
        form(action='/main' method='get')
            p 
                h2 검색하고 싶은 선수의 등번호를 입력해주세요.
            p 
                input.searcharea(type='text' name='backnumber')
            p 
                input.button(type='submit')
        br
        br
        div.table
            table
                thead
                    tr
                        each val, index in [...Array(10).keys()]
                            th Column #{index + 1}
                tbody
                    each r in [...Array(10).keys()]
                        tr
                            each c in [...Array(10).keys()]
                                td Cell #{r + 1},#{c + 1}